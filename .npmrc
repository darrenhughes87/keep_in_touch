# Disable pnpm's minimum-release-age supply-chain check.
# Default is ~1 day, which means freshly-published transitive deps (e.g. a
# new @babel/runtime patch) fail `pnpm install --frozen-lockfile` for ~24h.
# For a single-user self-hosted app, the noise outweighs the protection.
minimum-release-age=0
